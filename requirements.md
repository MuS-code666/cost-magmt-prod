# requirements.md

## Phase5: Claude（Anthropic API）への送信データポリシー

- 策定日: 2026-09-20
- 対象: 月報コメント自動生成、閾値超過アラート時の解釈コメント（Phase5、BigQuery MCP経由）

### 送信範囲

- データソースは BigQuery `cost-mgmt-prod-507701.mart.v_variance` のみとする。`history.*` `master.*` の生テーブルへは直接アクセスしない。
- 全件送信はしない。Claudeに送るのは以下の2種類に限定する。
  1. **ワーストN行**: 年月×部門（`factory_code`）内で `cost_budget_variance_rank <= 10`（部門ごと上位10件、Looker Studio P3と共通定義）に該当する行。`v_variance`の全列を対象とする（売上・利益系列を含む）。
  2. **部門集計値**: 年月×部門ごとの原価予算差異額合計・差異率（`cost_budget_variance_amount`のSUM、差異率は別途算出）。
- 対象年月は必ず絞り込む（`target_month`を指定しない全期間スキャンは行わない）。
- 今後`v_variance`に個人名に相当する列（例: 営業担当者名、作業員名）が追加された場合は、本ポリシーを改定し、当該列を送信対象から除外またはマスキングしてから送信対象に加えること。

### マスキング方針

- `customer_name`（顧客名・法人名）: マスキングなしでそのまま送信する。原価分析上必要な情報であり、個人情報には該当しないと判断。
- 個人名: `v_variance`には現状、個人名に相当する列は存在しない。
  - 参考: `master.sales_staff.sales_staff_name`（営業担当者名）、`master.workers.worker_name`（作業員名）は個人名の可能性があるが、`v_variance`はこれらのテーブルを結合していないため、現状は送信対象に含まれない。
  - 将来`v_variance`にこれらを結合する場合は、当該列を送信対象から除外するか、マスキング（イニシャル化・コード化等）してから送信すること。

### 送信先・利用目的

- 送信先: Anthropic API（Claude、BigQuery MCP経由での問い合わせ・分析を含む）。
- 利用目的は以下に限定する。
  - 月報コメントの生成（主要差異→要因→コメント→次月アクションの型）
  - 閾値超過アラート通知への解釈コメント添付（任意拡張）
- **Claudeは数値の再計算を行わない。** 金額・率などの数値はすべてBigQueryのクエリ結果をそのまま引用し、Claudeの役割は解釈・要因仮説・アクション提案に限定する。誤数値混入の防止策として、Task 5-5（出力検証）で生成コメント内の数値とv_varianceの値が一致することを3か月分突合する。

### 規約・社内規程との整合

- 本ポリシー策定時点（2026-09-20）で、Anthropic社の公開情報上はビジネス利用（API）のデータが既定では学習に使用されないことを確認済み。最新の利用規約は変更されうるため、実運用開始前および定期的に再確認し、変更があれば本欄の確認日を更新すること。
- 上記「外部送信してよいデータの範囲」を、本ポリシーをもって社内ルールとする。
- 本ポリシーを変更した場合は、下記の改訂履歴に日付・変更点を追記する。

### 改訂履歴

| 日付 | 変更内容 |
| --- | --- |
| 2026-09-20 | 初版策定 |
